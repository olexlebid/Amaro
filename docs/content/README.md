# Content

Тут лежать дані й фінальні тексти для сайту.

- Усі тексти, що підуть на сайт, — тільки німецькою.
- `courses.csv` — розклад і дані курсів (один рядок = одне заняття на тиждень).
- `team.csv` — команда; `photoFile` = ім'я файлу в `/_source/images/team/`.
- Тексти сторінок — окремий файл на сторінку: `startseite.md`, `hochzeitstanz.md` тощо.
